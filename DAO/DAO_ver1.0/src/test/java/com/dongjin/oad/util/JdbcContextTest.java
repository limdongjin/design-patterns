package com.dongjin.oad.util;

import org.junit.Assert;
import org.junit.Before;
import org.junit.Test;

import javax.sql.DataSource;

import static org.mockito.Mockito.mock;

public class JdbcContextTest {
    @Before
    public void setUp() {

    }

    @Test
    public void executeSql() {
        //DataSourceImplOnlyConnection dataSource = new DataSourceImplOnlyConnection();
        //dataSource.setConnection();

//        JdbcContext jdbcContext = new JdbcContext(dataSource);
////        ArrayList result =  jdbcContext.executeSql("", null);
////        assertEquals(result, new ArrayList<String>());

//        DataSource dataSource = mock(DataSourceImplOnlyConnection.class);
//        System.out.println(dataSource);
//        System.out.println(dataSource.getConnection());
    }
}